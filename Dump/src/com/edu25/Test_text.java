package com.edu25;

public class Test_text {
	/* 5.
	 * What is the name of the Java concept 
	 * that uses access modifiers to protect 
	 * variables and hide them within a class?
	 * -> 변수를 보호하고 클래스 내에 숨기기 위해 액세스 한정자를 사용하는
	 * Java 개념의 이름은 무엇입니까?
	 * 
	 * Encapsulation
	 * -> 캡슐화
	 * 
	 * 15.
	 * Which statement best describes encapsulation?
	 * -> 다음 중 캡슐화를 가장 잘 설명한 문장은 무엇입니까?
	 * 
	 * A. Encapsulation ensures that classes 
	 * can be designed so that only certain 
	 * fields and methods of an object are 
	 * accessible from other objects.
	 * -> 캡슐화는 객체의 특정 필드와 메소드만 다른 객체에서 액세스할 수 있는
	 * 클래스를 설계 할 수 있도록 보장합니다.
	 * 
	 * 
	 * 31.
	 * Which three are advantages of the Java exception mechanism?
	 * 자바 예외 메커니즘의 세 가지 장점은 무엇입니까?
	 * 장점1 : 일반 코드에서 오류 처리 코드 분리
	 * 장점2 : 호출 스택 위로 오류 전파
	 * 장점3 : 오류 유형 그룹화 및 구별
	 * 
	 * A. Improves the program structure because the error handling 
	 * code is separated from the normal program function
	 * -> 에러 처리 코드가 정상적인 프로그램 기능과 분리되어 프로그램 구조를 개선함
	 * 
	 * C. Improves the program structure because the programmer can 
	 * choose where to handle exceptions
	 * -> 프로그래머가 예외를 처리할 위치를 선택할 수 있으므로 프로그램 구조가 개선됨
	 * 
	 * E. Allows the creation of new exceptions that are 
	 * tailored to the particular program being created
	 * -> 생성 중인 특정 프로그램에 맞게 조정된 새 예외를 만들 수 있다
	 * 
	 * 40.Which three statements describe the object-oriented 
	 * features of the Java language?
	 * -> 자바 언어의 객체 지향적 특징을 설명하는 세 문장은 무엇입니까?
	 * 
	 * A subclass can inherit from a superclass.
	 * -> 하위 클래스는 상위 클래스로부터 상속될 수 있습니다.
	 * 
	 * Objects can share behaviors with other objects.
	 * -> 객체는 다른 객체와 동작을 공유할 수 있습니다.
	 * 
	 * Object is the root class of all other objects.
	 * -> 오브젝트는 다른 모든 객체의 루트 클래스입니다.
	 * 
	 * 64.
	 * Given the code fragment: int[] array = {1, 2, 3, 4, 5};
	 * And given the requirements:
	 * 1. Process all the elements of 
	 * the array in the order of entry.
	 * -> 배열의 모든 요소를 항목 순서대로 처리합니다.
	 * 
	 * 2. Process all the elements of 
	 * the array in the reverse order of entry.
	 * -> 배열의 모든 요소를 항목의 역순으로 처리합니다.
	 * 
	 * 3. Process alternating elements of 
	 * the array in the order of entry.
	 * -> 배열의 교대 요소를 항목 순서대로 처리합니다.
	 * 
	 * B. Requirements 1, 2, and 3 can be implemented 
	 * by using the standard for loop.
	 * -> 요구사항 1,2,3번은 루프에 대한 표준을 사용하여 실행할 수 있습니다.
	 * 
	 * D. Requirement 1 can be implemented by 
	 * using the enhanced for loop.
	 * -> 요구사항 1번은 강화된 루프를 사용하여 구현할 수 있습니다.
	 * 
	 * 67.
	 * Which statement is true about Java byte code?
	 * -> 다음 중 자바 바이트 코드에 대한 설명으로 옳은 것은?
	 * 바이트코드란 자바 가상 머신이 이해할 수 있는 언어로 변환된 자바 소스 코드를 의미
	 * 자바 바이트 코드는의 확장자는 .class이며 자바 가상 머신만 설치되어 있으면,
	 * 어떤 운영체제에서라도 실행될 수 있습니다.
	 * 
	 * It can run on any platform that has the Java Runtime Environment.
	 * -> Java Runtime Environment가 있는 모든 플랫폼에서 실행할 수 있습니다.
	 * -> 추가 설명하면 바이트 코드는 이미 컴파일이 된 파일이기 때문에 JRE가 있어야 한다
	 * 
	 * 74.
	 * Which two are benefits of polymorphism?
	 * -> 다형성 장점 두 가지는 무엇입니까?
	 * C. More dynamic code at runtime
	 * -> 런타임에 더 많은 동적 코드
	 * D. More flexible and reusable code
	 * -> 보다 유연하고 재사용 가능한 코드
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 */
	
}
